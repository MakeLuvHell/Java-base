# W1 / W2 评分量规（导师用）

目标：评价「理解与工程习惯」，不是「堆了多少行」。

---

## W1 环境 + 跟读（建议总分 100）

| 维度 | 分值 | 合格线 | 优秀 |
| --- | --- | --- | --- |
| 环境可演示 | 25 | admin 登录 + 用户管理有数据 | checklist 齐全、问题记录清楚 |
| 登录 4 问书面 | 25 | 4 问都说到要点、非整段粘贴 | 能对应到类/Redis 键前缀 |
| 用户列表路径表 | 20 | API→Controller→权限字符正确 | Service/Mapper 也填对 |
| Swagger | 10 | 带 Token 调通需登录接口 | 对比无 Token 401 |
| 口头 ≥6 题 | 15 | ≥5 题合格且第 8 题（前端藏按钮）必过 | 能展开讲三层权限 |
| 工程红线 | 5 | 无密钥进 Git | 分支命名规范、提问质量高 |

**建议通过：** ≥70，且环境可演示、红线通过、口头第 8 题合格。  
**不通过常见因：** 环境未绿硬写作业；认为前端隐藏=安全；作业纯复制。

### W1 书面批改快判

| 信号 | 判断 |
| --- | --- |
| 有钥匙牌/行李或等价比喻 | 理解 JWT+Redis 分工 |
| 写出 `Authorization` / Bearer / Admin-Token | 前端带身份清楚 |
| 路径表权限是 `system:user:list` | 跟过代码 |
| 与 `docs/demo` 一字不差 | 要求当面指代码复述 |

---

## W2 垂直切片（建议总分 100）

| 维度 | 分值 | 合格线 | 优秀 |
| --- | --- | --- | --- |
| 设计先行 | 15 | 第 1 天交设计且获 OK 后再写 | 风险与范围写清 |
| P0 功能 | 35 | 存得住、列表置顶优先、UI 可改 | 边界值/默认值稳妥 |
| 权限 403 | 20 | 无 edit 直调 403 | 账号准备与自测步骤可复现 |
| 代码质量 | 15 | 分层合理、XML/实体一致、未误改 isRead | 无无关大 diff |
| PR / Demo | 10 | SQL+自测+权限说明齐全 | Demo 5～10 分钟清晰 |
| 红线 | 5 | 无密钥、未去 PreAuthorize、未动 Security 核心 | — |

**建议通过：** ≥70，且 P0 全满足、403 真实测过、红线通过。  
**一票否决：** 去掉 `@PreAuthorize`；提交真实密码；把 `isRead` 当置顶且拒不改。

---

## 反馈话术（可复制）

**通过 W1：**  
> 环境与主链路跟读达标。下周发 W2，只做 P0 置顶；第 1 天先交设计。

**W1 口头第 8 题不过：**  
> 今天先回去重看 zero-basics 权限三节 + 自己路径表结论句，明天补考第 7～8 题，通过后再进 W2。

**W2 设计 OK：**  
> 设计按 is_top + SQL 排序 + 复用 edit 通过，可以改库写代码。注意别动 isRead。

**W2 需修改：**  
> 功能方向对，请补：（1）Mapper order by （2）无权限 403 自测证据 （3）PR 里的 SQL。改完再约 Review。

---

## 进度看板（可选）

| 姓名 | W1 环境 | W1 书面 | W1 口头 | W2 设计 | W2 PR | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |
